<div class="container">
<h1>Lista de equipas</h1>
</div>
 
<div class="container">
<table>
  <tr>
    <th>ID</th>
    <th>Nome</th>
    <th>Nº de Colaboradores</th>
  </tr>
 
  <% @groups.each do |g| %>
    <tr>
      <td><%= g.id %></td>
      <td><%= g.title %></td>
      <td><%= g.collaborators.count %></td>
    </tr>
  <% end %>
</table>
</div>

<div class="container" style="margin-top:30px;">
  <%= link_to 'Adicionar nova', new_group_path %>
</div>

<style type="text/css">

tr > td {
  padding-top: 5px;
  padding-right: 10px;
}

</style>